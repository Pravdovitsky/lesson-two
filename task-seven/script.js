const sum = (firstValue, secondValue) => firstValue + secondValue;
const division = (firstValue, secondValue) => firstValue / secondValue;
const difference = (firstValue, secondValue) => firstValue - secondValue;
const multiplication = (firstValue, secondValue) => firstValue * secondValue;

sum(2, 2);
division(6, 3);
difference(7, 1);
multiplication(2, 3);
